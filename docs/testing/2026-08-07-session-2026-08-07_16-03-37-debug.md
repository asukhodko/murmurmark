# Session Debug Cycle: 2026-08-07_16-03-37

Date: 2026-08-07

## Scope

Full local debug cycle for a 98-minute real meeting. The run checked capture durability, cached ASR,
review closure, selected-profile reconciliation and the anonymous remote-speaker chain. Raw audio was
treated as immutable; this note contains metrics only and does not reproduce meeting text.

## Capture And ASR

- wall-clock audio coverage: `5897.418s`;
- mic and remote tracks cover the complete session;
- explicit stop, no capture warnings, restarts or timeline gaps;
- mic raw SHA-256 remained
  `af06aa254c0a1e765ddf8033e3ece93d61d6c969e641a4571e7c1f2eb8b5c345`;
- remote raw SHA-256 remained
  `dfb7779e1656e73fc4923087c8080bb12dac22701e59546251727ea5e8d7712e`;
- ASR chunk cache is complete: `99/99` mic and `99/99` remote chunks;
- first authoritative processing took `2659.3s`; a checkpointed resume took `3.132s` and did not
  rerun ASR.

## Selected Transcript

- selected profile: `reviewed_v1`;
- selected utterances: `814`;
- review decisions: `75/75`, with `72 keep_me`, `3 drop_me`, `0` remaining;
- one unsupported opening mic fragment was removed by evidence-backed review;
- lifecycle reports `manual_decisions.total = 0` while retaining `40` conservative quality flags;
- local-only island recall is `0.916667`;
- the seven missing local-only islands are non-blocking: five are remote-covered (`3.53s`) and two
  are short (`0.91s`); `possible_lost_me = 0`.

The legacy remote-leak segment plan contained 11 rows / `82.54s`. Reconciliation against the
current selected dialogue reduces it to one current row / `11.55s`: ten rows / `70.99s` are already
resolved and no row is stale. The remaining interval has conflicting local-ASR and remote-similarity
evidence, so it remains an explicit non-actionable quality flag. Whole-utterance deletion would not
be evidence-safe.

## Anonymous Remote Speakers

Remote Speaker Coverage v3 was rebuilt from the current `reviewed_v1` dialogue rather than reused
from an older transcript profile.

- published anonymous speakers: `3`;
- attributed remote words: `6067/6289`;
- attributable remote speech ratio: `0.946258`;
- remaining aggregate words: `222` / `190.967115s`;
- text, `Me`, role, timestamp, order and word-conservation gates: all passed;
- v2 inputs are current and the promoted policy is active.

This is positive evidence for Speaker-Resolved Transcript Default v1: eligible sessions can render
the current v3 view automatically, while unsupported words remain aggregate `Colleagues`. It is not
evidence for assigning human names or for accepting stale rich artifacts.

## Fixes Proven By The Cycle

- Target-Me refresh now consumes exact utterance-bounded lane clips and rejects stale text/session
  evidence while preserving valid cached rows.
- Review suggestions may use independent double-talk evidence without weakening remote-like vetoes.
- remote-speaker artifacts are bound to the Evidence Handoff selected transcript profile;
  `transcript --rich` rejects a stale profile.
- lifecycle separates completed manual decisions from residual quality flags.
- session readiness reconciles legacy remote-leak plans with the current selected dialogue instead
  of presenting removed or reviewed rows as current work.

## Result

The session is readable and reproducible with a closed actionable review queue. Its remaining
`review_first` state is an honest policy result caused by one unresolved segment and conservative
full-transcript review gates, not by a hidden manual queue. Raw capture and all frozen inputs remain
unchanged.
